spring:
  application:
    name: springboot-cicd

  datasource:
    url: jdbc:h2:mem:taskdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  flyway:
    enabled: false  # Enabled per-profile; use H2 create-drop for dev

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics,env
  endpoint:
    health:
      show-details: when-authorized
      livenessstate:
        enabled: true
      readinessstate:
        enabled: true
  metrics:
    export:
      prometheus:
        enabled: true
  info:
    env:
      enabled: true

app:
  version: "@project.version@"
  rate-limit:
    enabled: false
  jwt:
    secret: ${JWT_SECRET:dev-secret-must-be-changed-in-production-minimum32chars}
    expiration-ms: 86400000
    refresh-expiration-ms: 604800000

info:
  app:
    name: "@project.name@"
    version: "@project.version@"
    description: "@project.description@"

logging:
  level:
    root: INFO
    com.devops.app: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
