# NOTE: This is a template — never commit real secrets.
# Use Sealed Secrets, External Secrets Operator, or inject via CI.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: springboot-cicd
type: Opaque
stringData:
  database-url: "REPLACE_WITH_SEALED_SECRET"
  database-user: "REPLACE_WITH_SEALED_SECRET"
  database-password: "REPLACE_WITH_SEALED_SECRET"
